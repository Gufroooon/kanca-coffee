<x-admin-layout>
    <div class="space-y-6" x-data="{ rows: [{ ingredient_id: '', quantity: '' }], editRows: [], edit: null }">
        <div class="flex flex-col md:flex-row md:justify-between gap-4"><div><p class="text-sm text-kanca-orange font-bold uppercase tracking-wider">Inventory</p><h1 class="text-2xl font-extrabold">Mixed Ingredients</h1><p class="text-sm text-gray-500 mt-1">Simpan formula bahan gabungan dan produksi dengan pemotongan stok proporsional.</p></div><button @click="$dispatch('open-modal', 'create-mixed')" class="px-4 py-3 rounded-xl bg-kanca-orange text-white text-sm font-bold">+ Buat Formula</button></div>
        <form class="bg-white dark:bg-zinc-900 rounded-2xl p-4 border border-gray-100 dark:border-zinc-800 flex gap-3"><input name="search" value="{{ request('search') }}" placeholder="Cari mixed ingredient..." class="flex-1 rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><button class="px-5 rounded-xl bg-zinc-900 text-white text-sm font-bold">Cari</button></form>
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-5">@forelse($mixedIngredients as $mixed)<div class="bg-white dark:bg-zinc-900 rounded-2xl p-5 border border-gray-100 dark:border-zinc-800"><div class="flex justify-between gap-3"><div><h2 class="font-extrabold">{{ $mixed->name }}</h2><p class="text-xs text-gray-500">Hasil: {{ $mixed->output_quantity }} {{ $mixed->unit }}</p></div><span class="px-2 py-1 h-fit rounded-full text-[10px] font-bold uppercase {{ $mixed->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-gray-100 text-gray-600' }}">{{ $mixed->is_active ? 'Aktif' : 'Nonaktif' }}</span></div><div class="mt-4 space-y-2">@foreach($mixed->items as $item)<div class="flex justify-between text-sm"><span>{{ $item->ingredient->name }}</span><strong>{{ $item->quantity }} {{ $item->ingredient->unit }}</strong></div>@endforeach</div><div class="mt-5 pt-4 border-t border-gray-100 dark:border-zinc-800 flex gap-2"><button @click="edit = {{ $mixed->toJson() }}; editRows = edit.items.map(item => ({ ingredient_id: item.ingredient_id, quantity: item.quantity })); $dispatch('open-modal', 'edit-mixed')" class="py-2 px-3 rounded-xl bg-amber-100 text-amber-800 text-xs font-bold">Edit</button><button @click="edit = {{ $mixed->toJson() }}; $dispatch('open-modal', 'produce-mixed')" class="flex-1 py-2 rounded-xl bg-kanca-teal text-white text-xs font-bold">Catat Produksi</button><form method="POST" action="{{ route('admin.mixed.destroy', $mixed) }}" onsubmit="return confirm('Hapus atau nonaktifkan formula ini?')">@csrf @method('DELETE')<button class="px-3 py-2 rounded-xl bg-rose-50 text-rose-700 text-xs font-bold">Hapus</button></form></div></div>@empty<div class="lg:col-span-2 p-10 text-center bg-white dark:bg-zinc-900 rounded-2xl text-gray-500">Belum ada formula mixed ingredient.</div>@endforelse</div><div>{{ $mixedIngredients->links() }}</div>
        <div x-data="{ open: false }" @open-modal.window="if ($event.detail === 'create-mixed') open=true" x-show="open" x-cloak class="fixed inset-0 z-50 bg-black/50 p-4 flex items-center justify-center"><div @click.outside="open=false" class="bg-white dark:bg-zinc-900 rounded-2xl p-6 w-full max-w-2xl max-h-[90vh] overflow-y-auto"><h2 class="font-extrabold text-lg mb-4">Buat Formula</h2><form method="POST" action="{{ route('admin.mixed.store') }}" class="space-y-3">@csrf<input name="name" required placeholder="Nama mixed ingredient" class="w-full rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><div class="grid grid-cols-2 gap-3"><input name="unit" required placeholder="Satuan hasil" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><input type="number" step="0.001" min="0.001" name="output_quantity" required placeholder="Total hasil" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"></div><div class="space-y-2"><template x-for="(row, index) in rows" :key="index"><div class="grid grid-cols-[1fr_120px_36px] gap-2"><select name="ingredient_ids[]" x-model="row.ingredient_id" required class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><option value="">Pilih bahan</option>@foreach($ingredients as $ingredient)<option value="{{ $ingredient->id }}">{{ $ingredient->name }} ({{ $ingredient->unit }})</option>@endforeach</select><input type="number" step="0.001" min="0.001" name="quantities[]" x-model="row.quantity" required placeholder="Qty" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><button type="button" @click="rows.splice(index,1)" class="rounded-xl bg-rose-50 text-rose-600">×</button></div></template></div><button type="button" @click="rows.push({ ingredient_id: '', quantity: '' })" class="text-xs font-bold text-kanca-teal">+ Tambah bahan dasar</button><button class="w-full py-3 rounded-xl bg-kanca-orange text-white font-bold">Simpan Formula</button></form></div></div>
        <div x-data="{ open: false }" @open-modal.window="if ($event.detail === 'edit-mixed') open=true" x-show="open" x-cloak class="fixed inset-0 z-50 bg-black/50 p-4 flex items-center justify-center"><div @click.outside="open=false" class="bg-white dark:bg-zinc-900 rounded-2xl p-6 w-full max-w-2xl max-h-[90vh] overflow-y-auto"><h2 class="font-extrabold text-lg mb-4">Edit Formula</h2><form method="POST" :action="edit ? '{{ url('admin/mixed-ingredients') }}/' + edit.id : '#'" class="space-y-3">@csrf @method('PUT')<input name="name" :value="edit?.name" required class="w-full rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><div class="grid grid-cols-2 gap-3"><input name="unit" :value="edit?.unit" required class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><input type="number" step="0.001" min="0.001" name="output_quantity" :value="edit?.output_quantity" required class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"></div><template x-for="(row, index) in editRows" :key="index"><div class="grid grid-cols-[1fr_120px_36px] gap-2"><select name="ingredient_ids[]" x-model="row.ingredient_id" required class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><option value="">Pilih bahan</option>@foreach($ingredients as $ingredient)<option value="{{ $ingredient->id }}">{{ $ingredient->name }} ({{ $ingredient->unit }})</option>@endforeach</select><input type="number" step="0.001" min="0.001" name="quantities[]" x-model="row.quantity" required class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><button type="button" @click="editRows.splice(index,1)" class="rounded-xl bg-rose-50 text-rose-600">×</button></div></template><button type="button" @click="editRows.push({ ingredient_id: '', quantity: '' })" class="text-xs font-bold text-kanca-teal">+ Tambah bahan dasar</button><button class="w-full py-3 rounded-xl bg-kanca-teal text-white font-bold">Simpan Perubahan</button></form></div></div>
        <div x-data="{ open: false }" @open-modal.window="if ($event.detail === 'produce-mixed') open=true" x-show="open" x-cloak class="fixed inset-0 z-50 bg-black/50 p-4 flex items-center justify-center"><div @click.outside="open=false" class="bg-white dark:bg-zinc-900 rounded-2xl p-6 w-full max-w-lg"><h2 class="font-extrabold text-lg mb-4">Catat Produksi <span x-text="edit?.name"></span></h2><form method="POST" :action="edit ? '{{ url('admin/mixed-ingredients') }}/' + edit.id + '/produce' : '#'" class="space-y-3">@csrf<input type="number" step="0.001" min="0.001" name="quantity" required placeholder="Jumlah produksi" class="w-full rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><input type="date" name="produced_at" value="{{ now()->toDateString() }}" required class="w-full rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"><textarea name="notes" placeholder="Catatan" class="w-full rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800"></textarea><button class="w-full py-3 rounded-xl bg-kanca-teal text-white font-bold">Catat & Kurangi Stok</button></form></div></div>
    </div>
</x-admin-layout>
