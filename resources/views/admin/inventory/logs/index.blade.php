<x-admin-layout>
    <div class="space-y-6"><div class="flex flex-col md:flex-row md:justify-between gap-4"><div><p class="text-sm text-kanca-orange font-bold uppercase tracking-wider">Inventory</p><h1 class="text-2xl font-extrabold">Inventory Harian</h1><p class="text-sm text-gray-500 mt-1">Usage dihitung otomatis dari opening dikurangi closing.</p></div><div class="flex gap-2"><a href="{{ route('admin.inventory.logs.export', request()->query() + ['format' => 'pdf']) }}" class="px-4 py-3 rounded-xl bg-rose-600 text-white text-xs font-bold">Export PDF</a><a href="{{ route('admin.inventory.logs.export', request()->query() + ['format' => 'excel']) }}" class="px-4 py-3 rounded-xl bg-emerald-600 text-white text-xs font-bold">Export Excel</a></div></div>
        <form method="POST" action="{{ route('admin.inventory.logs.store') }}" class="bg-white dark:bg-zinc-900 rounded-2xl p-5 border border-gray-100 dark:border-zinc-800 grid grid-cols-1 md:grid-cols-6 gap-3">@csrf<select name="ingredient_id" required class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><option value="">Bahan</option>@foreach($ingredients as $ingredient)<option value="{{ $ingredient->id }}">{{ $ingredient->name }}</option>@endforeach</select><input type="date" name="log_date" value="{{ now()->toDateString() }}" required class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><input type="number" step="0.001" min="0" name="opening_stock" required placeholder="Opening" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><input type="number" step="0.001" min="0" name="closing_stock" required placeholder="Closing" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><input name="notes" placeholder="Catatan" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><button class="rounded-xl bg-kanca-orange text-white text-sm font-bold">Simpan Log</button></form>
        <form class="bg-white dark:bg-zinc-900 rounded-2xl p-4 border border-gray-100 dark:border-zinc-800 grid grid-cols-1 md:grid-cols-4 gap-3"><select name="ingredient_id" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><option value="">Semua bahan</option>@foreach($ingredients as $ingredient)<option value="{{ $ingredient->id }}" @selected(request('ingredient_id') == $ingredient->id)>{{ $ingredient->name }}</option>@endforeach</select><input type="date" name="date_from" value="{{ request('date_from') }}" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><input type="date" name="date_to" value="{{ request('date_to') }}" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><button class="rounded-xl bg-zinc-900 text-white text-sm font-bold">Filter</button></form>
        <div class="bg-white dark:bg-zinc-900 rounded-2xl border border-gray-100 dark:border-zinc-800 overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-gray-50 dark:bg-zinc-800 text-xs uppercase text-gray-500"><tr><th class="p-4">Tanggal</th><th class="p-4">Bahan</th><th class="p-4">Opening</th><th class="p-4">Closing</th><th class="p-4">Usage</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-zinc-800">@forelse($logs as $log)<tr><td class="p-4">{{ $log->log_date->format('d M Y') }}</td><td class="p-4 font-bold">{{ $log->ingredient->name }} <span class="text-xs text-gray-500">({{ $log->ingredient->unit }})</span></td><td class="p-4">{{ $log->opening_stock }}</td><td class="p-4">{{ $log->closing_stock }}</td><td class="p-4 font-bold text-kanca-orange">{{ $log->usage }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-gray-500">Belum ada inventory log.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $logs->links() }}</div></div>
    </div>
</x-admin-layout>
