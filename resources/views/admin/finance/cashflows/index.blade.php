<x-admin-layout>
    <div class="space-y-6"><div class="flex flex-col md:flex-row md:justify-between gap-4"><div><p class="text-sm text-kanca-orange font-bold uppercase tracking-wider">Keuangan</p><h1 class="text-2xl font-extrabold">{{ $type === 'income' ? 'Pemasukan' : 'Pengeluaran' }}</h1><p class="text-sm text-gray-500 mt-1">Catat dan filter transaksi {{ $type === 'income' ? 'pendapatan' : 'biaya operasional' }}.</p></div><div class="flex gap-2"><a href="{{ route('admin.finance.export', request()->query() + ['format' => 'pdf', 'type' => $type]) }}" class="px-3 py-2 rounded-xl bg-rose-600 text-white text-xs font-bold">PDF</a><a href="{{ route('admin.finance.export', request()->query() + ['format' => 'excel', 'type' => $type]) }}" class="px-3 py-2 rounded-xl bg-emerald-600 text-white text-xs font-bold">Excel</a></div></div>
        <form method="POST" action="{{ route('admin.cashflows.store', $type) }}" class="bg-white dark:bg-zinc-900 rounded-2xl p-5 border border-gray-100 dark:border-zinc-800 grid grid-cols-1 md:grid-cols-6 gap-3">@csrf<input type="date" name="transaction_date" value="{{ now()->toDateString() }}" required class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><input type="number" step="0.01" min="0.01" name="amount" required placeholder="Nominal" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm">@if($type === 'expense')<select name="category" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><option value="">Kategori</option>@foreach($categories as $category)<option>{{ $category }}</option>@endforeach</select>@else<input name="source" placeholder="Sumber pemasukan" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm">@endif<input name="description" placeholder="Deskripsi / catatan" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm md:col-span-2"><button class="rounded-xl bg-kanca-orange text-white text-sm font-bold">Simpan</button></form>
        <form class="bg-white dark:bg-zinc-900 rounded-2xl p-4 border border-gray-100 dark:border-zinc-800 grid grid-cols-1 md:grid-cols-5 gap-3"><input name="search" value="{{ request('search') }}" placeholder="Cari deskripsi..." class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><input type="date" name="date_from" value="{{ request('date_from') }}" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><input type="date" name="date_to" value="{{ request('date_to') }}" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm">@if($type === 'expense')<select name="category" class="rounded-xl border-gray-200 dark:border-zinc-700 dark:bg-zinc-800 text-sm"><option value="">Semua kategori</option>@foreach($categories as $category)<option @selected(request('category') === $category)>{{ $category }}</option>@endforeach</select>@endif<button class="rounded-xl bg-zinc-900 text-white text-sm font-bold">Filter</button></form>
        <div class="bg-white dark:bg-zinc-900 rounded-2xl border border-gray-100 dark:border-zinc-800 overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-gray-50 dark:bg-zinc-800 text-xs uppercase text-gray-500"><tr><th class="p-4">Tanggal</th><th class="p-4">Nominal</th><th class="p-4">Kategori/Sumber</th><th class="p-4">Deskripsi</th><th class="p-4 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-zinc-800">@forelse($cashflows as $cashflow)<tr><td class="p-4">{{ $cashflow->transaction_date->format('d M Y') }}</td><td class="p-4 font-extrabold {{ $type === 'income' ? 'text-emerald-600' : 'text-rose-600' }}">Rp {{ number_format((float) $cashflow->amount, 0, ',', '.') }}</td><td class="p-4">{{ $cashflow->category ?: $cashflow->source ?: '-' }}</td><td class="p-4 text-gray-600">{{ $cashflow->description ?: '-' }}</td><td class="p-4 text-right"><form method="POST" action="{{ route('admin.cashflows.destroy', $cashflow) }}" onsubmit="return confirm('Hapus transaksi ini?')">@csrf @method('DELETE')<button class="text-rose-600 font-bold text-xs">Hapus</button></form></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-gray-500">Belum ada transaksi.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $cashflows->links() }}</div></div>
    </div>
</x-admin-layout>
